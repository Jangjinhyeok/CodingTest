#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
    double A, B;

    cin >> A >> B;

    cout.precision(10); 
    cout << fixed;
    cout << A / B << endl;

	return 0;
}