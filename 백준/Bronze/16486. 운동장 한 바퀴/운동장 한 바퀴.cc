#include <bits/stdc++.h>
using namespace std;
int main()
{
    double a, b;
    cin >> a >> b;
    double answer = (a * 2) + (2 * 3.141592 * b);
    cout << fixed << setprecision(6) << answer << endl;
    return 0;
}