#include <bits/stdc++.h>
using namespace std;
int main() 
{
    double r;
    cin >> r;
    double eu = r * r * M_PI;
    double ta = 2.0 * r * r;
    cout << fixed << setprecision(6);
    cout << eu << endl;
    cout << ta << endl;
    return 0;
}